# Backup demo video (~5 min) — Rene / Sprint 3–4

**Purpose:** Prerecorded screen capture if the live final demo fails (network/API).

## Flow in one sentence

Budget → **Analyze** → quiz → **Grade** → scroll grounded explanation, tip, charts, **What-if** — works with or without Gemini; the results header shows **Output source** (e.g. deterministic fallback).

## Stable recording path

1. Two terminals: `python main.py` in `backend/`, `npm run dev` in `frontend/`.
2. Leave **`GEMINI_API_KEY`** empty for a repeatable **Deterministic fallback** label (see `docs/final_demo_script.md` Scenario 3).
3. Follow shot list in `docs/final_demo_script.md` (balanced path + optional overspend + fallback proof).

Numbers for the happy path: **final_demo_script.md** Scenario 1.

## After recording

Store the file where your team agrees (e.g. link in `docs/` or course submission folder). Check off **Record backup demo video** in `docs/Sprint 3.md` when done.
